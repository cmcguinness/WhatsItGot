{
  "name": "whatsitgot-extension",
  "version": "1.3.1",
  "description": "Chrome extension that detects what technologies a website is built with",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "fetch-fingerprints": "node scripts/fetch-fingerprints.js",
    "build": "npm run fetch-fingerprints"
  }
}
